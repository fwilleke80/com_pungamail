-- Punga Mail 0.3.2 uses the normalized topic-membership tables introduced in
-- 0.3.0. This marker keeps Joomla's recorded database version aligned with the
-- extension manifest; no schema change is required.

